---
description: 统一约定项目中图标使用 uni-icons 库
globs: **/*.vue
alwaysApply: false
---

# 图标统一使用 uni-icons 库

## 规则目的

- 统一项目中的图标来源与风格，**一律使用 uni-icons**。
- 避免重复造轮子（手写 CSS 图标、Base64 图片等）和引入不适合移动端的 UI 库（例如为图标而引入 Element UI）。

## 适用范围

- 所有 `*.vue` 页面和组件（UniApp/H5/小程序）。

## 使用约定

### 1. 图标组件

- 统一使用 `uni-icons` 组件：

```vue
<uni-icons type="home" size="24" color="#333"></uni-icons>
```

- **禁止**：
  - 使用纯 CSS 画图标（如边框+线条拼出来的 icon）。
  - 使用图片当作通用图标（除非是真实业务图片，如 LOGO、产品图等）。
  - 为图标单独引入 PC UI 库（如 Element UI）。

### 2. 属性规范

- **type**
  - 必须使用 `uni-icons` 官方支持的图标类型（如 `home`、`list`、`scan`、`compose`、`settings` 等）。
  - 同一业务语义在全项目内**保持统一的 type**，例如：
    - 设置：统一使用 `settings`
    - 编辑：统一使用 `compose`
    - 列表 / 订单：统一使用 `list`

- **size**
  - 使用数字形式，内部会自动转为适配单位：
  - 底部导航、工具栏推荐：`20 ~ 26`
  - 示例：`size="24"`

- **color**
  - 默认灰色：`#9ca3af`
  - 主题色：`#1a2a6c`
  - 推荐写法：根据激活状态动态切换颜色：

```vue
<uni-icons
  type="home"
  size="24"
  :color="isActive ? '#1a2a6c' : '#9ca3af'"
/>
```

### 3. 底部 Tab 图标示例

```vue
<view class="tab-item" :class="{ active: activeTab === 'order' }" @tap="activeTab = 'order'">
  <view class="tab-icon">
    <uni-icons
      type="list"
      size="24"
      :color="activeTab === 'order' ? '#1a2a6c' : '#9ca3af'"
    />
  </view>
  <text class="tab-label">订单设置</text>
</view>
```

建议默认映射：

- 订单设置：`type="list"`
- 扫码复检：`type="scan"`
- 信息修改：`type="compose"`
- 手动控制 / 配置：`type="settings"` 或 `type="tune"`

## 组件注册（推荐）

- 在入口处全局注册 `uni-icons`，避免在各个页面重复引入：

```js
// main.js 示例（Vue2 模式）
import Vue from 'vue'
import App from './App'
import uniIcons from '@/uni_modules/uni-icons/components/uni-icons/uni-icons.vue'

Vue.component('uni-icons', uniIcons)
```

注册后，所有 `.vue` 页面/组件都可以直接使用 `<uni-icons />`。

